# Données personnelles — positions, PRU, soldes, journal d'opérations.
# Le dépôt ne contient que les fichiers d'exemple (*.example.json).
data/*.json
!data/*.example.json

node_modules/
functions/node_modules/
*.log
.DS_Store
Thumbs.db

# Firebase CLI
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
pglite-debug.log
